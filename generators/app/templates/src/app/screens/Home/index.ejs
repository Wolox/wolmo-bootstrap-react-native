import React, { <%_ if(features.login) { _%>useCallback, <%_ } _%>memo } from 'react';
import { View } from 'react-native';
<%_ if(features.login) { _%>
import { useDispatch } from 'react-redux';
import CustomButton from '@components/CustomButton';
import { actionCreators as AuthActions } from '@redux/auth/actions';
<%_ } else { _%>
import CustomText from '@components/CustomText';
<%_ } _%>

import styles from './styles';

function Home() {
  <%_ if(features.login) { _%>
  const dispatch = useDispatch();
  const handleLogout = useCallback(() => dispatch(AuthActions.logout()), [dispatch]);
  <%_ } _%>
  return (
    <View style={styles.container}>
    <%_ if(features.login) { _%>
      <CustomButton onPress={handleLogout} green title="Logout!" style={styles.home} />
    <%_ } else { _%>
      <CustomText style={styles.home}><%= projectName %></CustomText>
    <%_ } _%>
    </View>
  );
}

export default memo(Home);
