import React from 'react';
import { View, Text<%_ if((features.drawerandroid || features.drawerios) && !features.login) { _%>, Button<%_ } %><%_ if(features.login) { _%>, TouchableOpacity<%_ } %> } from 'react-native';

import styles from './Home.styles';
<%_ if ((features.drawerandroid || features.drawerios) && !features.login) { _%>
import { actionCreators as drawerActions } from '../../redux/drawerHandlers';
<%_ } _%>

<%_ if(features.login) { _%>
export default function Home({ onLogout }) {
<%_ } else { _%>
export default function Home() {
<%_ } _%>
  return (
    <View style={styles.container}>
      <%_ if(features.login) { _%>
      <TouchableOpacity onPress={onLogout} style={styles.mainButton}>
        <Text>Logout!</Text>
      </TouchableOpacity>
      <%_ } else { _%>
      <Text style={styles.mainButton}><%= projectName %></Text>
      <%_ } _%>
    </View>
  );
}

<%_ if (features.login) { _%>
Home.propTypes = {
  onLogout: React.PropTypes.func.isRequired
};
<%_ } else { _%>
Home.navigationOptions = {
  <%_ if (features.drawerandroid || features.drawerios) { _%>
  header: ({ dispatch }) => ({
    right: <Button onPress={() => dispatch(drawerActions.drawerToggled())} title="Drawer" />
  }),
  <%_ } _%>
  title: 'Home'
};
<%_ } _%>
