import React from 'react';
import { View, Text<%_ if(features.login) { _%>, TouchableOpacity<%_ } %> } from 'react-native';

import styles from './Dashboard.styles';

<%_ if(features.login) { _%>
export default function Dashboard({ onLogout }) {
<%_ } else { _%>
export default function Dashboard() {
<%_ } _%>
  return (
    <View style={styles.container}>
      <%_ if(features.login) { _%>
      <TouchableOpacity onPress={onLogout} style={styles.mainButton}>
        <Text>Logout!</Text>
      </TouchableOpacity>
      <%_ } else { _%>
      <Text style={styles.mainButton}><%= projectName %></Text>
      <%_ } _%>
    </View>
  );
}

<%_ if(!features.login) { _%>
Dashboard.navigationOptions = {
  title: 'Dashboard'
};

<%_ } _%>
Dashboard.propTypes = {
  onLogout: React.PropTypes.func.isRequired
};
